---
interface Props {
  link: string;
  title: string;
}
const { link, title } = Astro.props;
---

<a
  href={link}
  class="bg-neutral-800 px-24 py-4 text-center transition-colors duration-300 hover:bg-neutral-700 sm:px-40"
  >{title}</a
>
